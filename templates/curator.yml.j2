---
# Remember, leave a key empty if there is no value.  None will be a string,
# not a Python "NoneType"
client:
  hosts:
    - {{ elasticsearch_curator_config_client_hosts }}
  port: {{ elasticsearch_curator_config_client_port }}
  url_prefix:
  use_ssl: False
  certificate:
  client_cert:
  client_key:
  ssl_no_validate: False
  http_auth:
  timeout: 30
  master_only: False

logging:
  loglevel: {{ elasticsearch_curator_config_logging_level }}
  logfile: {{ elasticsearch_curator_config_logging_logfile }}
  logformat: {{ elasticsearch_curator_config_logging_format }}
  blacklist: ['elasticsearch', 'urllib3']
...
